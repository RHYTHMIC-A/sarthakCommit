import * as express from 'express';
export declare enum RequestType {
    GET = "GET",
    PATCH = "PATCH",
    POST = "POST"
}
export interface IPreRoute {
    path: string;
    steps: Array<express.RequestHandler>;
}
export interface IRoute {
    type: RequestType;
    path: string;
    steps: Array<express.RequestHandler>;
}
export declare class Router {
    static createRoutes(type: RequestType, pre: IPreRoute[]): IRoute[];
    static mergeRoutes(routes: IRoute[][]): IRoute[];
    private router;
    constructor(...routes: IRoute[][]);
    readonly routes: express.Router;
}
